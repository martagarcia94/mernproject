{
  "name": "mernproject",
  "version": "1.0.0",
  "private": true,
  "proxy": "http://localhost8000/",
  "dependencies": {
  "concurrently": "^8.1.0" 
  },
  "scripts": {
    "installAll": "cd backend && npm install && cd ../frontend && npm install",
    "startAll": "concurrently \"cd backend && nodemon server.js\" \"cd frontend && npm start\""
  }
  }
